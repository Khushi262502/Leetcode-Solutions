class Solution(object):
    def duplicateNumbersXOR(self, nums):
        XOR = 0
        posses = set()
        for i in range(len(nums)):
            count = 0
            for j in range(i+1,len(nums)):
                if(nums[i]==nums[j]):
                    count = count+1
            if(count == 1 and nums[i] not in posses):
                XOR = XOR ^ nums[i]
                posses.add(nums[i])
        return XOR

Input: nums = [1,2,2,1]
Output: 3
Explanation:
Numbers 1 and 2 appeared twice. 1 XOR 2 == 3

