class Solution(object):
    def duplicateNumbersXOR(self, nums):
        freq={}
        xor=0
        for i in nums:
            if i not in freq:
                freq[i]=1
            else:
                xor^=i
        return xor

Input: nums = [1,2,2,1]
Output: 3
Explanation:
Numbers 1 and 2 appeared twice. 1 XOR 2 == 3

